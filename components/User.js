import React from "react";

export class User {
    name;
    role;
    id;
    email;
    password;
    signedEvents = {w: [], m: [], wa: [], nms: []}

    constructor(name, role, id, email, password) {
        
        
        // [this.name, setName] = useState(name);
        // [this.role, setRole] = useState(role);
        // [this.id, setID] = useState(id);
        // [this.email, setEmail] = useState(email);
        // [this.password, setPassword] = useState(password)
        
    }

   
}